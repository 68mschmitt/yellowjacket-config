# This file generates the GitHub Actions matrix.
# For simple board + shield combinations, add them to the top level board and
# shield arrays, for more control, add individual board + shield combinations
# to the `include` property. You can also use the `cmake-args` property to
# pass flags to the build command, `snippet` to add a Zephyr snippet, and
# `artifact-name` to assign a name to distinguish build outputs from each other:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#   - board: nice_nano_v2
#     shield: corne_left
#     snippet: studio-rpc-usb-uart
#     cmake-args: -DCONFIG_ZMK_STUDIO=y
#     artifact-name: corne_left_with_studio
#
---
include:
  # ========== Keyboard 1 (YJ1) ==========
  - board: nice_nano
    shield: yellowjacket_left
    snippet: studio-rpc-usb-uart
    cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_ZMK_KEYBOARD_NAME="YJ1"
    artifact-name: yj1_left
  - board: nice_nano
    shield: yellowjacket_right
    cmake-args: -DCONFIG_ZMK_KEYBOARD_NAME="YJ1"
    artifact-name: yj1_right

  # ========== Keyboard 2 (YJ2) ==========
  - board: nice_nano
    shield: yellowjacket_left
    snippet: studio-rpc-usb-uart
    cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_ZMK_KEYBOARD_NAME="YJ2"
    artifact-name: yj2_left
  - board: nice_nano
    shield: yellowjacket_right
    cmake-args: -DCONFIG_ZMK_KEYBOARD_NAME="YJ2"
    artifact-name: yj2_right

  # ========== Utilities ==========
  - board: nice_nano
    shield: settings_reset

  # ========== Debug (optional) ==========
  - board: nice_nano
    shield: yellowjacket_left
    snippet: zmk-usb-logging
    cmake-args: -DCONFIG_ZMK_KEYBOARD_NAME="YJ1"
    artifact-name: yj1_left_logging
